name: Aplicar correção Fibra

on:
  push:
    paths:
      - 'scripts/patch_fibra_pdf.py'
      - '.github/workflows/apply-fibra-fix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  aplicar:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Aplicar parser Fibra
        run: python scripts/patch_fibra_pdf.py
      - name: Validar sintaxe
        run: python -m py_compile app.py
      - name: Commit da correção
        run: |
          if git diff --quiet -- app.py; then
            echo "Sem alterações"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app.py
          git commit -m "Corrige leitura de PDF do Banco Fibra em todas as ferramentas"
          git push
